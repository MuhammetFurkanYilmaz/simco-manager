## 🔧 Hotfix v1.1.17

- **Build Pipeline Final Fix:** Reconfigured the automated deployment pipeline to use Windows (instead of Ubuntu) to match the local build environment perfectly. This resolves the remaining obfuscation inconsistencies that were causing random runtime errors in the Chrome version on GitHub Releases.
